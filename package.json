{
  "name": "taskforge-root",
  "version": "1.0.0",
  "description": "TaskForge Root workspace manager",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npx --yes concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  }
}
